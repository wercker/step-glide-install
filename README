Use glide to install your Go dependencies, then run `go build`

Just add:

  - termie/glide-build

You can disable the build behavior by adding build: false

  - termie/glide-build
      build: false
