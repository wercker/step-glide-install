# glide-install

Uses [glide](https://github.com/Masterminds/glide) to setup a `$GOPATH` and
install your Go dependencies.

# Example

```yaml
build:
    steps:
         - glide-install
```

# License

The MIT License (MIT)
